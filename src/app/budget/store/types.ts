/**
 * An indicator type that the given date string
 * should always be in ISO date format.
 */
export type ISODateString = string

/**
 * An indicator type that the given currency
 * is of the format #.## and can be correctly converted
 * to a real currency() object
 */
export type CurrencyString = string